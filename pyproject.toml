[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gama"
version = "1.0.0"
dependencies = [
    "typer",
    "rich",
    "docker",
    "psutil"
]

[project.scripts]
gama = "gama.main:app"

[tool.setuptools]
packages = ["gama"]